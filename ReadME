Pred files are based on CRF

Process:
1. To get “twitter_dev.ner.pred” and “twitter_dev_test.ner.pred” respectively, run the below command.
    python main.py -T crf -e data/twitter_dev.ner data/twitter_dev_test.ner
2. To get “twitter_test.ner.pred", run the below command
    python main.py -T crf -e data/twitter_test.ner 

The above commands run the CRF tagger, that utilizes the viterbi algorithm.
All the necessary source files are included in the submission, expect the data files.
